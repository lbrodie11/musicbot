# #!/usr/bin/env bash
# now \
# && sleep 5 \
# && now alias \
# && now scale entertainmentlackey.now.sh bru1 0 \
# && now scale entertainmentlackey.now.sh sfo1 1 \
# && echo y | now rm $(now ls entertainment-lackey | egrep "entertainment-lackey-.*now\.sh" -o | tail -n1)